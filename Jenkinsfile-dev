pipeline {
  agent any
    stages {
      stage("React Build") {
        steps {
          sh "ls"
          sh "pnpm install"
          sh "pnpm build"
        }
    }
    stage('Finish') {
      steps {
        sh 'docker restart react'
      }
    }
  }
}
