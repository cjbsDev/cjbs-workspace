pipeline {
  agent any
    stages {
      stage("React Build") {
        steps {
          sh "cd / pnpm install"
          sh "cd apps/bsadmin pnpm build"
        }
    }
    stage('Finish') {
      steps {
        sh 'docker restart react'
      }
    }
  }
}
