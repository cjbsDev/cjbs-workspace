pipeline {
  agent any
    stages {
      stage("React Build") {
        steps {
          sh "pnpm install"
          sh "turbo run build --filter=bsadmin…"
        }
    }
    stage('Finish') {
      steps {
        sh 'docker restart react'
      }
    }
  }
}
